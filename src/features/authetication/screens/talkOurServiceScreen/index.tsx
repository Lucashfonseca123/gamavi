import {memo} from 'react';
import {Text, View} from 'react-native';

const TalkOutServiceScreen = () => {
  return (
    <View>
      <Text>This is TalkOutServiceScreen</Text>
    </View>
  );
};

export default memo(TalkOutServiceScreen);
