import {memo} from 'react';
import {Text, View} from 'react-native';

const PasswordScreen = () => {
  return (
    <View>
      <Text>This is PasswordScreen</Text>
    </View>
  );
};

export default memo(PasswordScreen);
