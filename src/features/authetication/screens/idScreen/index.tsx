import {memo} from 'react';
import {Text, View} from 'react-native';

const IdScreen = () => {
  return (
    <View>
      <Text>This is IdScreen</Text>
    </View>
  );
};

export default memo(IdScreen);
